import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function ProfilePage() {
  return (
    <PagePlaceholder title="Profile" phase="Phase 1 — Onboarding & targets">
      Your details, living situation (with hostel mode), goals, and calculated
      BMR/TDEE and daily calorie, macro, and water targets.
    </PagePlaceholder>
  );
}
