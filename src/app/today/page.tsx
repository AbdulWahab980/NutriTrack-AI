import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function TodayPage() {
  return (
    <PagePlaceholder title="Today" phase="Phase 4 — Daily dashboard">
      Daily totals vs. targets with progress rings for calories, protein, and
      water will live here.
    </PagePlaceholder>
  );
}
